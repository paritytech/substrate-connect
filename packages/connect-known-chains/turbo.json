{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "inputs": ["src/**", "tsconfig.json", "scripts/**", "specs/**"],
      "outputs": ["dist/**", "src/specs/**"]
    }
  }
}
