{
  "name": "@substrate/extension",
  "version": "0.2.9",
  "description": "Browser extension to manage substrate-based blockchain light clients",
  "main": "dist/src/index.js",
  "author": "Parity Technologies",
  "license": "GPL-3.0-only",
  "type": "module",
  "scripts": {
    "prep:m3": "mkdir -p dist && cp -r ./assets/icons ./dist && cp ./assets/manifest-v3.json ./dist/manifest.json",
    "prep:m2": "mkdir -p dist && cp -r ./assets/icons ./dist && cp ./assets/manifest-v2.json ./dist/manifest.json",
    "build": "yarn build:m3 && yarn build:m2",
    "build:m3": "yarn clean && yarn prep:m3 && concurrently \"yarn:build-*\" && cd dist && zip -r packed-m3-extension.zip .",
    "build:m2": "yarn clean && yarn prep:m2 && concurrently \"yarn:build-*\" && cd dist && zip -r packed-m2-extension.zip .",
    "build-content": "INPUT=content npx vite build --config vite.script.config.js",
    "build-background": "INPUT=background npx vite build --config vite.script.config.js",
    "build-ui": "vite build --config vite.ui.config.js",
    "dev": "yarn dev:m3",
    "dev:m3": "yarn clean && yarn prep:m3 && concurrently \"yarn:dev-*\"",
    "dev:m2": "yarn clean && yarn prep:m2 && concurrently \"yarn:dev-*\"",
    "dev-content": "yarn build-content --mode development --watch",
    "dev-background": "yarn build-background --mode development --watch",
    "dev-ui": "yarn build-ui --mode development --watch",
    "lint": "eslint src --ext .js,.jsx,.ts,.tsx",
    "test": "vitest",
    "start": "web-ext run --source-dir ./dist -t chromium",
    "start:firefox": "web-ext run --source-dir ./dist -t firefox-desktop",
    "clean": "rm -rf dist",
    "deep-clean": "yarn clean && rm -rf node_modules",
    "bundle": "yarn run bundle:m3 && yarn run bundle:m2",
    "bundle:m3": "cp tsconfig.json tsconfig_bkp.json && mv tsconfig.ext.json tsconfig.json && yarn run clean && rm -rf node_modules && zip -r substrate-connect_source-code.zip . -x 'node_modules/*' 'dist/*' 'tsconfig_bkp.json' && mv tsconfig_bkp.json tsconfig.json && mv substrate-connect_m3-source-code.zip ../..",
    "bundle:m2": "cp tsconfig.json tsconfig_bkp.json && mv tsconfig.ext.json tsconfig.json && yarn run clean && rm -rf node_modules && zip -r substrate-connect_source-code.zip . -x 'node_modules/*' 'dist/*' 'tsconfig_bkp.json' && mv tsconfig_bkp.json tsconfig.json && mv substrate-connect_m2-source-code.zip ../.."
  },
  "keywords": [],
  "devDependencies": {
    "@vitejs/plugin-react-swc": "^3.3.2",
    "autoprefixer": "^10.4.14",
    "concurrently": "^8.2.1",
    "eslint": "^8.49.0",
    "postcss": "^8.4.25",
    "tailwindcss": "^3.3.2",
    "typescript": "^5.2.2",
    "vite": "^4.4.9",
    "vitest": "^0.34.3",
    "web-ext": "^7.6.2"
  },
  "dependencies": {
    "@substrate/connect": "workspace:^",
    "@substrate/connect-extension-protocol": "workspace:^",
    "@unstoppablejs/utils": "^1.1.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-icons": "^4.10.1",
    "scale-ts": "^1.4.1",
    "smoldot": "2.0.1"
  }
}
