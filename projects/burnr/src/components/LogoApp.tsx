import React from 'react';
import { Typography } from '@material-ui/core';

const LogoApp: React.FunctionComponent = () => {
	return  <Typography variant='h1'>Burnr</Typography>;
};

export default LogoApp;
